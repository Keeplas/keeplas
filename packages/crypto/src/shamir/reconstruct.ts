/**
 * Reconstruct a secret from t or more shares.
 *
 * TODO: Implement Lagrange interpolation over GF(256) in Phase 2.
 */
export async function reconstruct(
  _shares: Uint8Array[]
): Promise<Uint8Array> {
  throw new Error("Not implemented — Phase 2");
}
